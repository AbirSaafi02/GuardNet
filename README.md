# GuardNet

Project structure:

- `backend/` contains the FastAPI API, database layer, services, routers, schemas, and `ia/`.
- `frontend/` contains the Angular application.

Common commands:

```bash
uvicorn backend.main:app --reload
```

```bash
cd frontend
npm install
npm start
```
